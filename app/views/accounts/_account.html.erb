  <li id="account-<%= account.id %>">
  <%= link_to gravatar_for(account.user, size: 50), account.user %>
  <span class="user">
    申請者:
    <%= link_to account.user.name, account.user %></span>
      <span class="name">
        作成するアカウント名:
        <%= account.name %>
      </span>
 </span>
  <span class="reason">
    申請理由:
    <%= account.reason %>
  </span>
 
  <span class="detail">
    <%= link_to "申請詳細", account_path(account) ,class: 'btn btn-default'%>
  </span>
  
　<span class="timestamp">
    <%= time_ago_in_words(account.created_at) %>前
  </span>

  

  